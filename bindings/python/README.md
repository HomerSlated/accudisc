# accudisc — Python bindings

Planned: `cffi`-based bindings generated from `include/accudisc/accudisc.h`.

Will be scaffolded (pyproject, build backend, CI wheels) once the C API
surface stabilizes past the initial `c2read` import.
